{
  "name": "VeAIOps",
  "scripts": {
    "dev": "nuxt dev --extends docus --port 4000",
    "build": "nuxt build --extends docus",
    "generate": "nuxt generate --extends docus"
  },
  "dependencies": {
    "docus": "latest",
    "better-sqlite3": "^12.2.0",
    "nuxt": "^4.1.0"
  }
}
